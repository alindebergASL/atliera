# Hermes handoff status

Current state: **OWNER-APPROVED v3.1 AMENDMENT — NEW EXACT C1 HANDOFF REQUIRED**.

Andrew Lindeberg approved the visual direction on August 18, 2026, then amended it to preserve the four predecessor visual-generation v2 references as an expressive floor and retain Workshop as a future calm account-local mode. This packet remains calibration rather than implementation authority; only the separate exact v3.1 C1 prompt carries bounded authorization.

The prior v3 archive and v3 Hermes prompt are superseded for any C1 handoff that has not started. Do not use both packets. If Hermes already began from v3, stop and return a precise restart/owner-decision status rather than hot-swapping calibration mid-slice.

## What Hermes must not infer from this packet

- Do not start or continue C1 unless the exact owner-issued v3.1 C1 prompt accompanies this packet and its identities verify.
- Do not commit this packet unless that prompt explicitly authorizes the repository-native calibration record.
- Do not treat future C2/C3/C4/C7/C9 concepts as current scope.
- Do not implement or display a Workshop tab, placeholder, or route in C1. Workshop remains in the product direction but is future until separately authorized with real content and an authorization boundary.
- Do not render, mount, hide, stub, or route to the future C3 composer controls shown in `visuals/04-prepare-composer-future-c3.png`.
- Do not import unproven dynamic, AI, monitoring, review, freshness, or navigation claims from the predecessor visual-generation v2 references.
- Do not implement Today monitoring, AI synthesis, Prepare, conflict recording, snapshots, identity, routes, persistence, or deployment.
- Do not treat any screenshot as account truth, a fixture, or a pixel-copy requirement.

## Authorized next step

Use the separately supplied replacement, tightly bounded C1 prompt that:

1. pins the merged calm-pivot main commit and tree;
2. identifies this packet by archive and manifest digest;
3. imports the owner-approved packet as repository visual calibration only;
4. implements only a local/test read-only Account Home projection over already-admitted input;
5. uses the C1 portions of Visuals 03, 07, 08, and the exact Visual 09 tokens;
6. preserves the four lineage visuals as the expressive/originality floor without copying their capability claims;
7. preserves future Workshop architecture while proving the literal absence of a live Workshop tab and C3 composer controls in C1;
8. does not implement future C2/C3/C4/C7/C9 behavior;
9. requires exact-head Product/UX, Architecture/Trust, accessibility/security, responsive, and full-CI proof;
10. opens one draft PR and stops without merge.

The replacement prompt should end with `READY_FOR_EXPLICIT_C1_CALM_ACCOUNT_HOME_REVIEW` or a precise blocked state. No merge or C2 work is implied.
